/**
 * Gitea-related constants
 */

/**
 * Default bot name used when none is configured.
 * On Gitea there is no built-in Claude App, so a generic default is used.
 */
export const GITEA_DEFAULT_BOT_NAME = "claude-bot";

/**
 * Default bot ID placeholder.
 */
export const GITEA_DEFAULT_BOT_ID = 0;
